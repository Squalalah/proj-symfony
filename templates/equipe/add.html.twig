{% extends 'template.html.twig' %}

{% block main %}

    {{ form_start(formAdd)  }}
    {{ form_widget(formAdd) }}
    <input type="submit" value="Ajouter">
    {{ form_end(formAdd) }}

{% endblock %}
