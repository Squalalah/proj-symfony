<form method="post" action="{{ path('idee_delete', {'id': idee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ idee.id) }}">
    <button class="btn">Delete</button>
</form>
