<form method="post" action="{{ path('categ_delete', {'id': categ.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categ.id) }}">
    <button class="btn">Delete</button>
</form>
